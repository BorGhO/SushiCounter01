<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keepon.sushicounter01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sushi_counter_icon01"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sushi_counter_icon01_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SushiCounter01">
        <activity android:name=".SecondActivity"></activity>
        <activity android:name=".MenuRecycler"></activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>